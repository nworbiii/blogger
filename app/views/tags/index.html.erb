<h1>All Tags</h1>
<ul class="tags">
	<% @tags.each do |tag| %>
		<li>
			<%= link_to tag, tag_path(tag), class: "tag_name" %>
		</li>
	<% end %>
</ul>
<% if Tag.all.empty? %>
	<p>There are currently no tags.</p>
<% end %>
<br>
<%= link_to "<< Back to Articles List", articles_path %>
